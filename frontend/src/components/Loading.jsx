function Loading() {
  return (
    <div
      style={{
        minHeight: "200px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#6b7890",
      }}
    >
      Loading...
    </div>
  );
}

export default Loading;